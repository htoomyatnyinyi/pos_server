### ============================================
### AUTHENTICATION
### ============================================

### 1. Register
POST http://localhost:6060/api/auth/register
Content-Type: application/json

{
  "name": "HTOO MYAT",
  "email": "htomyat@gmail.com",
  "password": "password123"
}

### 2. Login
# @name login
POST http://localhost:6060/api/auth/login
Content-Type: application/json

{
  "email": "htomyat@gmail.com",
  "password": "password123"
}

### 3. Get Current User
# @name me
GET http://localhost:6060/api/auth/me
Authorization: Bearer {{login.response.body.token}}

### ============================================
### CONSTANTS
### ============================================
@tenantId = cmqroot6b000301jv9vdzo18u
@userId = cmqroot6i000501jvkrnuf4o8
@storeId = cmqroot6e000401jv779mrcvs

### ============================================
### TENANTS
### ============================================

### 4. Get All Tenants
GET http://localhost:6060/api/tenants/
Authorization: Bearer {{login.response.body.token}}

### 5. Get Tenant by ID
GET http://localhost:6060/api/tenants/{{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 6. Create a Tenant
POST http://localhost:6060/api/tenants/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "code": "TENANT-002",
  "name": "New Tenant",
  "email": "newtenant@example.com",
  "phone": "09123456789"
}

### ============================================
### STORES
### ============================================

### 7. Get All Stores
GET http://localhost:6060/api/stores/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 8. Get Store by ID
GET http://localhost:6060/api/stores/{{storeId}}
Authorization: Bearer {{login.response.body.token}}

### 9. Create a Store
# @name createStore
POST http://localhost:6060/api/stores/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "code": "STORE-002",
  "name": "New Store",
  "address": "456 New Street, Yangon",
  "phone": "09123456788",
  "email": "newstore@example.com",
  "taxNumber": "TAX-123456"
}

###
@newStoreId = {{createStore.response.body.id}}

### 10. Update Store
PUT http://localhost:6060/api/stores/{{storeId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "Updated Store Name",
  "address": "789 Updated Street",
  "phone": "09987654321",
  "isActive": true
}

### 11. Delete Store
DELETE http://localhost:6060/api/stores/{{newStoreId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### CATEGORIES
### ============================================

### 12. Get All Categories
GET http://localhost:6060/api/categories/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 13. Create a Category
# @name createCategory
POST http://localhost:6060/api/categories/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "name": "Beverages",
  "slug": "beverages",
  "description": "Hot and cold drinks",
  "sortOrder": 1
}

@categoryId = {{createCategory.response.body.id}}

### 14. Get Category by ID
GET http://localhost:6060/api/categories/{{categoryId}}
Authorization: Bearer {{login.response.body.token}}

### 15. Update Category
PUT http://localhost:6060/api/categories/{{categoryId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "Beverages & Drinks",
  "description": "All types of beverages",
  "isActive": true
}

### 16. Delete Category
DELETE http://localhost:6060/api/categories/{{categoryId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### PRODUCTS
### ============================================

### 17. Get All Products
GET http://localhost:6060/api/products/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 18. Create a Product (without variants)
# @name createProduct
POST http://localhost:6060/api/products/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "userId": "{{userId}}",
  "sku": "COF-001",
  "barcode": "1234567890123",
  "name": "Espresso Coffee",
  "description": "Strong black coffee",
  "brand": "Premium Roast",
  "costPrice": 1.50,
  "sellingPrice": 3.50,
  "categoryId": "{{categoryId}}",
  "storeId": "{{storeId}}",
  "initialStock": 100
}

@productId = {{createProduct.response.body.id}}

### 19. Create a Product with Variants
# @name createProductWithVariants
POST http://localhost:6060/api/products/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "userId": "{{userId}}",
  "sku": "COF-002",
  "barcode": "1234567890124",
  "name": "Espresso Coffee (Variants)",
  "description": "Strong black coffee with size options",
  "brand": "Premium Roast",
  "costPrice": 1.50,
  "sellingPrice": 3.50,
  "categoryId": "{{categoryId}}",
  "storeId": "{{storeId}}",
  "initialStock": 100,
  "variants": [
    {
      "name": "Small",
      "price": 2.50,
      "costPrice": 1.00,
      "sku": "COF-002-S",
      "barcode": "1234567890125",
      "color": "Brown",
      "size": "S",
      "weight": 0.25,
      "isActive": true
    },
    {
      "name": "Medium",
      "price": 3.50,
      "costPrice": 1.50,
      "sku": "COF-002-M",
      "barcode": "1234567890126",
      "color": "Brown",
      "size": "M",
      "weight": 0.35,
      "isActive": true
    },
    {
      "name": "Large",
      "price": 4.50,
      "costPrice": 2.00,
      "sku": "COF-002-L",
      "barcode": "1234567890127",
      "color": "Brown",
      "size": "L",
      "weight": 0.45,
      "isActive": true
    }
  ]
}

@productVariantId = {{createProductWithVariants.response.body.id}}

### 20. Get Product by ID
GET http://localhost:6060/api/products/{{productId}}
Authorization: Bearer {{login.response.body.token}}

### 21. Get Product by Barcode
GET http://localhost:6060/api/products/barcode/1234567890123
Authorization: Bearer {{login.response.body.token}}

### 22. Update Product
PUT http://localhost:6060/api/products/{{productId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "sellingPrice": 4.00,
  "isActive": true
}

### 23. Delete Product
DELETE http://localhost:6060/api/products/{{productId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### SUPPLIERS
### ============================================

### 24. Get All Suppliers
GET http://localhost:6060/api/suppliers/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 25. Create a Supplier
# @name createSupplier
POST http://localhost:6060/api/suppliers/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "code": "SUPP-001",
  "name": "Coffee Beans Co., Ltd.",
  "contactName": "Mr. John Roaster",
  "phone": "09987654321",
  "email": "roaster@coffeebeans.com",
  "address": "123 Industrial Zone, Yangon",
  "paymentTerms": 30,
  "creditLimit": 5000
}

@supplierId = {{createSupplier.response.body.id}}

### 26. Get Supplier by ID
GET http://localhost:6060/api/suppliers/{{supplierId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 27. Update Supplier
PUT http://localhost:6060/api/suppliers/{{supplierId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "Premium Coffee Beans Co.",
  "phone": "09876543210",
  "isActive": true
}

### 28. Delete Supplier
DELETE http://localhost:6060/api/suppliers/{{supplierId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### CUSTOMERS
### ============================================

### 29. Get All Customers
GET http://localhost:6060/api/customers/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 30. Create a Customer
# @name createCustomer
POST http://localhost:6060/api/customers/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "code": "CUST-001",
  "name": "John Doe",
  "phone": "09123456789",
  "email": "john.doe@example.com",
  "address": "456 Main Street, Yangon",
  "tier": "GOLD"
}

@customerId = {{createCustomer.response.body.id}}

### 31. Get Customer by ID
GET http://localhost:6060/api/customers/{{customerId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 32. Update Customer
PUT http://localhost:6060/api/customers/{{customerId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "John Doe Jr.",
  "phone": "09123456788",
  "tier": "PLATINUM",
  "loyaltyPoints": 1000,
  "isActive": true
}

### 33. Delete Customer
DELETE http://localhost:6060/api/customers/{{customerId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### PURCHASE ORDERS
### ============================================

### 34. Get All Purchase Orders
GET http://localhost:6060/api/purchase-orders/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 35. Create a Purchase Order
# @name createPO
POST http://localhost:6060/api/purchase-orders/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "supplierId": "{{supplierId}}",
  "status": "DRAFT",
  "subTotal": 150.00,
  "taxAmount": 7.50,
  "grandTotal": 157.50,
  "userId": "{{userId}}",
  "items": [
    {
      "productId": "{{productId}}",
      "quantity": 100,
      "unitCost": 1.50,
      "totalCost": 150.00
    }
  ]
}

@poId = {{createPO.response.body.id}}

### 36. Get Purchase Order by ID
GET http://localhost:6060/api/purchase-orders/{{poId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 37. Update Purchase Order
PUT http://localhost:6060/api/purchase-orders/{{poId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "status": "ORDERED",
  "notes": "Urgent order - need by Friday"
}

### 38. Receive Purchase Order (Increments Stock)
POST http://localhost:6060/api/purchase-orders/{{poId}}/receive
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "storeId": "{{storeId}}",
  "userId": "{{userId}}"
}

### 39. Delete Purchase Order
DELETE http://localhost:6060/api/purchase-orders/{{poId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### ORDERS / SALES
### ============================================

### 40. Get All Orders
GET http://localhost:6060/api/orders/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 41. Create an Order
# @name createOrder
POST http://localhost:6060/api/orders/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "subTotal": 7.00,
  "taxAmount": 0.50,
  "discountAmount": 0.00,
  "grandTotal": 7.50,
  "paymentMethod": "CASH",
  "paidAmount": 10.00,
  "changeAmount": 2.50,
  "paymentStatus": "PAID",
  "userId": "{{userId}}",
  "customerId": "{{customerId}}",
  "storeId": "{{storeId}}",
  "items": [
    {
      "productId": "{{productId}}",
      "quantity": 2,
      "unitPrice": 3.50,
      "subTotal": 7.00
    }
  ]
}

@orderId = {{createOrder.response.body.id}}

### 42. Get Order by ID
GET http://localhost:6060/api/orders/{{orderId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 43. Update Order Status
PATCH http://localhost:6060/api/orders/{{orderId}}/status
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "status": "COMPLETED"
}

### 44. Delete Order
DELETE http://localhost:6060/api/orders/{{orderId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### PAYMENTS
### ============================================

### 45. Get All Payments
GET http://localhost:6060/api/payments/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 46. Create a Payment
# @name createPayment
POST http://localhost:6060/api/payments/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "orderId": "{{orderId}}",
  "amount": 7.50,
  "method": "KBZ_PAY",
  "referenceNumber": "TXN-123456789",
  "status": "COMPLETED",
  "processedById": "{{userId}}"
}

@paymentId = {{createPayment.response.body.id}}

### 47. Get Payment by ID
GET http://localhost:6060/api/payments/{{paymentId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 48. Update Payment
PUT http://localhost:6060/api/payments/{{paymentId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "status": "REFUNDED",
  "referenceNumber": "REF-123456"
}

### 49. Delete Payment
DELETE http://localhost:6060/api/payments/{{paymentId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### RETURNS
### ============================================

### 50. Get All Returns
GET http://localhost:6060/api/returns/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 51. Create a Return
POST http://localhost:6060/api/returns/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "orderId": "{{orderId}}",
  "customerId": "{{customerId}}",
  "totalAmount": 3.50,
  "refundMethod": "CASH",
  "refundStatus": "APPROVED",
  "reason": "Wrong item delivered",
  "approvedById": "{{userId}}",
  "items": [
    {
      "orderItemId": "{{createOrder.response.body.items[0].id}}",
      "quantity": 1,
      "refundAmount": 3.50,
      "reason": "Damaged product"
    }
  ]
}

### 52. Get Return by ID
GET http://localhost:6060/api/returns/{{orderId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 53. Delete Return
DELETE http://localhost:6060/api/returns/{{orderId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### INVENTORY MANAGEMENT
### ============================================

### 54. Get Inventory
GET http://localhost:6060/api/inventory/?tenantId={{tenantId}}&storeId={{storeId}}
Authorization: Bearer {{login.response.body.token}}

### 55. Get Stock Movements
GET http://localhost:6060/api/inventory/movements/?tenantId={{tenantId}}&storeId={{storeId}}
Authorization: Bearer {{login.response.body.token}}

### 56. Create Stock Movement (Manual Adjustment)
POST http://localhost:6060/api/inventory/movements/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "storeId": "{{storeId}}",
  "productId": "{{productId}}",
  "quantity": 10,
  "type": "ADJUSTMENT",
  "referenceId": "MANUAL-ADJ-001",
  "referenceType": "MANUAL",
  "reason": "Stock reconciliation",
  "userId": "{{userId}}"
}

### 57. Get Inventory Counts
GET http://localhost:6060/api/inventory/counts/?tenantId={{tenantId}}&storeId={{storeId}}
Authorization: Bearer {{login.response.body.token}}

### 58. Create Inventory Count
POST http://localhost:6060/api/inventory/counts/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "storeId": "{{storeId}}",
  "scheduledDate": "2026-12-31T00:00:00Z",
  "userId": "{{userId}}",
  "items": [
    {
      "productId": "{{productId}}",
      "systemQuantity": 100,
      "countedQuantity": 98,
      "reason": "Shrinkage"
    }
  ]
}

### ============================================
### STOCK TRANSFERS
### ============================================

### 59. Get All Stock Transfers
GET http://localhost:6060/api/stock-transfers/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 60. Create Stock Transfer
# @name createTransfer
POST http://localhost:6060/api/stock-transfers/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "fromStoreId": "{{storeId}}",
  "toStoreId": "{{storeId}}",
  "userId": "{{userId}}",
  "notes": "Transfer for seasonal demand",
  "items": [
    {
      "productId": "{{productId}}",
      "quantity": 20
    }
  ]
}

@transferId = {{createTransfer.response.body.id}}

### 61. Get Stock Transfer by ID
GET http://localhost:6060/api/stock-transfers/{{transferId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 62. Complete Stock Transfer
POST http://localhost:6060/api/stock-transfers/{{transferId}}/complete
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "userId": "{{userId}}"
}

### 63. Delete Stock Transfer
DELETE http://localhost:6060/api/stock-transfers/{{transferId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### SESSIONS (Cashier Sessions)
### ============================================

### 64. Get All Sessions
GET http://localhost:6060/api/sessions/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 65. Get Active Session for User
GET http://localhost:6060/api/sessions/active/{{userId}}
Authorization: Bearer {{login.response.body.token}}

### 66. Open Cashier Session
# @name openSession
POST http://localhost:6060/api/sessions/open
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "userId": "{{userId}}",
  "openingBalance": 500.00,
  "storeId": "{{storeId}}",
  "notes": "Morning shift"
}

@sessionId = {{openSession.response.body.id}}

### 67. Close Cashier Session
POST http://localhost:6060/api/sessions/{{sessionId}}/close
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "closingBalance": 1500.00,
  "expectedBalance": 1450.00,
  "discrepancy": 50.00,
  "cashSales": 800.00,
  "cardSales": 400.00,
  "digitalSales": 250.00,
  "notes": "End of day closing"
}

### ============================================
### STAFF MANAGEMENT
### ============================================

### 68. Get All Staff
GET http://localhost:6060/api/staff/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 69. Get Staff by ID
GET http://localhost:6060/api/staff/{{userId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 70. Create Staff Member
# @name createStaff
POST http://localhost:6060/api/staff/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "username": "cashier_john",
  "email": "john@minipos.com",
  "name": "John Smith",
  "password": "securepass123",
  "storeId": "{{storeId}}",
  "role": "CASHIER",
  "permissions": ["VIEW_REPORTS", "EDIT_PRICES"],
  "isActive": true
}

@staffId = {{createStaff.response.body.id}}

### 71. Update Staff Member
PUT http://localhost:6060/api/staff/{{staffId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "John M. Smith",
  "role": "MANAGER",
  "permissions": ["VIEW_REPORTS", "EDIT_PRICES", "VOID_ORDERS"],
  "isActive": true
}

### 72. Delete Staff Member
DELETE http://localhost:6060/api/staff/{{staffId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### PROMOTIONS
### ============================================

### 73. Get All Promotions
GET http://localhost:6060/api/promotions/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 74. Create a Promotion
# @name createPromotion
POST http://localhost:6060/api/promotions/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "code": "SUMMER2026",
  "name": "Summer Sale 2026",
  "description": "10% off all beverages",
  "discountType": "PERCENTAGE",
  "discountValue": 10.00,
  "minPurchase": 5.00,
  "startDate": "2026-06-01T00:00:00Z",
  "endDate": "2026-08-31T00:00:00Z",
  "usageLimit": 1000,
  "perUserLimit": 5,
  "isActive": true,
  "productIds": ["{{productId}}"]
}

@promotionId = {{createPromotion.response.body.id}}

### 75. Get Promotion by ID
GET http://localhost:6060/api/promotions/{{promotionId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 76. Update Promotion
PUT http://localhost:6060/api/promotions/{{promotionId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "discountValue": 15.00,
  "endDate": "2026-09-30T00:00:00Z",
  "isActive": true
}

### 77. Delete Promotion
DELETE http://localhost:6060/api/promotions/{{promotionId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### TAX RATES
### ============================================

### 78. Get All Tax Rates
GET http://localhost:6060/api/tax-rates/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 79. Create Tax Rate
# @name createTaxRate
POST http://localhost:6060/api/tax-rates/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "name": "VAT 5%",
  "rate": 5.00,
  "isCompound": false,
  "appliesTo": ["GROCERY", "BEVERAGES"],
  "validFrom": "2026-01-01T00:00:00Z",
  "validTo": "2026-12-31T00:00:00Z"
}

@taxId = {{createTaxRate.response.body.id}}

### 80. Get Tax Rate by ID
GET http://localhost:6060/api/tax-rates/{{taxId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 81. Update Tax Rate
PUT http://localhost:6060/api/tax-rates/{{taxId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "rate": 7.00,
  "isActive": true
}

### 82. Delete Tax Rate
DELETE http://localhost:6060/api/tax-rates/{{taxId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### EXPENSES
### ============================================

### 83. Get All Expenses
GET http://localhost:6060/api/expenses/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 84. Get Expense Categories
GET http://localhost:6060/api/expenses/categories/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 85. Create Expense Category
# @name createExpenseCategory
POST http://localhost:6060/api/expenses/categories/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "name": "Utilities",
  "description": "Electricity, water, internet bills"
}

@expenseCategoryId = {{createExpenseCategory.response.body.id}}

### 86. Update Expense Category
PUT http://localhost:6060/api/expenses/categories/{{expenseCategoryId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "Utilities & Bills",
  "description": "All utility bills",
  "isActive": true
}

### 87. Delete Expense Category
DELETE http://localhost:6060/api/expenses/categories/{{expenseCategoryId}}
Authorization: Bearer {{login.response.body.token}}

### 88. Create Expense
# @name createExpense
POST http://localhost:6060/api/expenses/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "categoryId": "{{expenseCategoryId}}",
  "amount": 150.00,
  "userId": "{{userId}}",
  "storeId": "{{storeId}}",
  "description": "Monthly electricity bill",
  "expenseDate": "2026-06-24T00:00:00Z"
}

@expenseId = {{createExpense.response.body.id}}

### 89. Get Expense by ID
GET http://localhost:6060/api/expenses/{{expenseId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 90. Update Expense
PUT http://localhost:6060/api/expenses/{{expenseId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "amount": 175.00,
  "description": "Monthly electricity bill (updated)"
}

### 91. Delete Expense
DELETE http://localhost:6060/api/expenses/{{expenseId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### CASH REGISTERS
### ============================================

### 92. Get All Cash Registers
GET http://localhost:6060/api/cash-registers/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 93. Create Cash Register
# @name createRegister
POST http://localhost:6060/api/cash-registers/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "storeId": "{{storeId}}",
  "name": "Main Register #1",
  "status": "OPEN"
}

@registerId = {{createRegister.response.body.id}}

### 94. Get Cash Register by ID
GET http://localhost:6060/api/cash-registers/{{registerId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 95. Update Cash Register
PUT http://localhost:6060/api/cash-registers/{{registerId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "Main Register Updated",
  "status": "CLOSED"
}

### 96. Delete Cash Register
DELETE http://localhost:6060/api/cash-registers/{{registerId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### GIFT CARDS
### ============================================

### 97. Get All Gift Cards
GET http://localhost:6060/api/gift-cards/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 98. Create Gift Card
# @name createGiftCard
POST http://localhost:6060/api/gift-cards/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "initialAmount": 100.00,
  "cardNumber": "GIFT-2026-001",
  "pinCode": "1234",
  "customerId": "{{customerId}}",
  "expiresAt": "2027-12-31T00:00:00Z"
}

@giftCardId = {{createGiftCard.response.body.id}}

### 99. Lookup Gift Card by Card Number
GET http://localhost:6060/api/gift-cards/lookup/GIFT-2026-001
Authorization: Bearer {{login.response.body.token}}

### 100. Reload Gift Card
POST http://localhost:6060/api/gift-cards/{{giftCardId}}/reload
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "amount": 50.00
}

### 101. Update Gift Card Status
PATCH http://localhost:6060/api/gift-cards/{{giftCardId}}/status
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "status": "USED"
}

### ============================================
### WALLETS
### ============================================

### 102. Get All Wallets
GET http://localhost:6060/api/wallets/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 103. Get Wallet by Customer ID
GET http://localhost:6060/api/wallets/customer/{{customerId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 104. Create Wallet Transaction
POST http://localhost:6060/api/wallets/transactions
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "customerId": "{{customerId}}",
  "amount": 25.00,
  "type": "DEPOSIT",
  "referenceId": "REF-WALLET-001",
  "description": "Customer deposit for future purchases"
}

### ============================================
### SUPPLIER PAYMENTS
### ============================================

### 105. Get All Supplier Payments
GET http://localhost:6060/api/supplier-payments/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 106. Create Supplier Payment
# @name createSupplierPayment
POST http://localhost:6060/api/supplier-payments/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "supplierId": "{{supplierId}}",
  "amount": 500.00,
  "paymentMethod": "BANK_TRANSFER",
  "referenceNumber": "BANK-TRF-001",
  "note": "Payment for coffee bean delivery"
}

@supplierPaymentId = {{createSupplierPayment.response.body.id}}

### 107. Get Supplier Payment by ID
GET http://localhost:6060/api/supplier-payments/{{supplierPaymentId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### API KEYS
### ============================================

### 108. Get All API Keys
GET http://localhost:6060/api/api-keys/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 109. Create API Key
# @name createApiKey
POST http://localhost:6060/api/api-keys/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "userId": "{{userId}}",
  "name": "Integration Service",
  "permissions": ["READ_PRODUCTS", "READ_ORDERS"],
  "expiresAt": "2027-12-31T00:00:00Z"
}

@apiKeyId = {{createApiKey.response.body.id}}

### 110. Delete API Key
DELETE http://localhost:6060/api/api-keys/{{apiKeyId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### WEBHOOKS
### ============================================

### 111. Get All Webhooks
GET http://localhost:6060/api/webhooks/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 112. Create Webhook
# @name createWebhook
POST http://localhost:6060/api/webhooks/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "name": "Order Notifications",
  "url": "https://webhook.site/abcdef123",
  "events": ["ORDER_CREATED", "ORDER_COMPLETED"],
  "secret": "webhook_secret_key_123"
}

@webhookId = {{createWebhook.response.body.id}}

### 113. Get Webhook by ID
GET http://localhost:6060/api/webhooks/{{webhookId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 114. Update Webhook
PUT http://localhost:6060/api/webhooks/{{webhookId}}
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "name": "Order Notifications - Updated",
  "isActive": true
}

### 115. Delete Webhook
DELETE http://localhost:6060/api/webhooks/{{webhookId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### NOTIFICATIONS
### ============================================

### 116. Get All Notifications
GET http://localhost:6060/api/notifications/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 117. Get Notifications by User
GET http://localhost:6060/api/notifications/?userId={{userId}}
Authorization: Bearer {{login.response.body.token}}

### 118. Create Notification
# @name createNotification
POST http://localhost:6060/api/notifications/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "userId": "{{userId}}",
  "type": "LOW_STOCK",
  "title": "Low Stock Alert",
  "message": "Product Espresso Coffee is running low!",
  "metadata": {
    "productId": "{{productId}}",
    "currentStock": 5
  }
}

@notificationId = {{createNotification.response.body.id}}

### 119. Get Notification by ID
GET http://localhost:6060/api/notifications/{{notificationId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 120. Mark Notification as Read
PATCH http://localhost:6060/api/notifications/{{notificationId}}/read
Authorization: Bearer {{login.response.body.token}}

### 121. Delete Notification
DELETE http://localhost:6060/api/notifications/{{notificationId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### AUDIT LOGS
### ============================================

### 122. Get All Audit Logs
GET http://localhost:6060/api/audit-logs/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 123. Create Audit Log
POST http://localhost:6060/api/audit-logs/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "userId": "{{userId}}",
  "action": "CREATE",
  "entity": "PRODUCT",
  "entityId": "{{productId}}",
  "newData": {
    "name": "Espresso Coffee",
    "price": 3.50
  },
  "ipAddress": "192.168.1.1",
  "userAgent": "Mozilla/5.0 (REST Client)"
}

### 124. Get Audit Log by ID
GET http://localhost:6060/api/audit-logs/{{auditLogId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### ============================================
### STORE SETTINGS
### ============================================

### 125. Get All Store Settings
GET http://localhost:6060/api/store-settings/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 126. Create Store Setting
# @name createStoreSetting
POST http://localhost:6060/api/store-settings/
Content-Type: application/json
Authorization: Bearer {{login.response.body.token}}

{
  "tenantId": "{{tenantId}}",
  "storeId": "{{storeId}}",
  "settingKey": "currency",
  "settingValue": "USD",
  "description": "Store currency setting",
  "userId": "{{userId}}"
}

@storeSettingId = {{createStoreSetting.response.body.id}}

### 127. Get Store Setting by ID
GET http://localhost:6060/api/store-settings/{{storeSettingId}}/?tenantId={{tenantId}}
Authorization: Bearer {{login.response.body.token}}

### 128. Delete Store Setting
DELETE http://localhost:6060/api/store-settings/{{storeSettingId}}
Authorization: Bearer {{login.response.body.token}}
